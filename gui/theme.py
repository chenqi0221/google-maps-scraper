"""主题配置模块，集中管理所有视觉常量 - Win11 毛玻璃风格"""

from typing import Dict, Tuple

# ============================================================
# Win11 毛玻璃色彩方案
# ============================================================

# 品牌色
BRAND_COLORS = {
    "primary": "#3B82F6",      # 主色调：亮蓝
    "secondary": "#64748B",    # 次要色：灰蓝
    "success": "#10B981",      # 成功：翠绿
    "warning": "#F59E0B",      # 警告：橙色
    "danger": "#EF4444",       # 危险：红色
    "info": "#60A5FA",         # 信息：天蓝
}

# 背景色 - 深色毛玻璃
BACKGROUND_COLORS = {
    "sidebar": "#0F172A",      # 侧边栏背景
    "main": "#0F172A",         # 主区域背景
    "card": "#1E293B",         # 卡片背景
    "card_hover": "#334155",   # 卡片悬停
    "log": "#0F172A",          # 日志背景
    "glass": "#1E293B",        # 毛玻璃基础色
}

# 文字色
TEXT_COLORS = {
    "primary": "#F1F5F9",      # 主文字 - 灰白
    "secondary": "#94A3B8",    # 次要文字 - 灰蓝
    "inverse": "#FFFFFF",      # 反色文字
    "muted": "#64748B",        # 弱化文字
    "accent": "#60A5FA",       # 强调文字
}

# 边框色
BORDER_COLORS = {
    "default": "#334155",      # 默认边框
    "hover": "#475569",        # 悬停边框
    "active": "#3B82F6",       # 激活边框
    "glass": "rgba(255,255,255,0.1)",  # 毛玻璃边框
}

# ============================================================
# 间距
# ============================================================
SPACING = {
    "xs": 4,
    "sm": 8,
    "md": 16,
    "lg": 24,
    "xl": 32,
}

# ============================================================
# 字体 - Segoe UI (Win11 默认字体)
# ============================================================
FONTS = {
    "title": ("Segoe UI", 20, "bold"),
    "subtitle": ("Segoe UI", 14, "bold"),
    "body": ("Segoe UI", 11),
    "mono": ("Consolas", 10),
    "small": ("Segoe UI", 9),
}

# ============================================================
# 圆角
# ============================================================
BORDER_RADIUS = 12

# ============================================================
# 日志颜色映射
# ============================================================
LOG_COLORS = {
    "success": "#10B981",
    "info": "#60A5FA",
    "warning": "#F59E0B",
    "error": "#EF4444",
}

# ============================================================
# 图标尺寸
# ============================================================
ICON_SIZES = {
    "small": (16, 16),
    "medium": (24, 24),
    "large": (32, 32),
}

# ============================================================
# 毛玻璃效果配置
# ============================================================
GLASS_EFFECT = {
    "bg": "#1E293B",
    "alpha": 0.85,
    "border_color": "#334155",
    "border_width": 1,
    "shadow": "0 8px 32px rgba(0,0,0,0.3)",
    "blur_radius": 20,
}

# ============================================================
# 侧边栏配置
# ============================================================
SIDEBAR_CONFIG = {
    "expanded_width": 220,
    "collapsed_width": 60,
    "animation_duration": 300,  # ms
    "item_height": 44,
    "icon_size": 20,
}

# ============================================================
# 响应式断点
# ============================================================
RESPONSIVE = {
    "sm": 600,   # 小屏幕 - 简化布局
    "md": 900,   # 中屏幕 - 折叠侧边栏
    "lg": 1100,  # 大屏幕 - 展开侧边栏
    "xl": 1400,  # 超大屏幕 - 固定展开
}
